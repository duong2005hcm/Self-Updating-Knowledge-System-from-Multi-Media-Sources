# Backend Node (auth + proxy RAG: /api/ask, ingest, user upload)
VITE_API_URL=http://localhost:5001

# Preferred base URL for frontend service calls (contact form, etc.)
# Example: http://localhost:6001
VITE_API_BASE_URL=http://localhost:5001

# FastAPI trực tiếp — danh sách/xóa tài liệu (chưa proxy qua Node)
VITE_RAG_API=http://localhost:8000

# n8n webhooks (Deep research + Image generator)
VITE_N8N_DEEP_RESEARCH_URL=http://localhost:5678/webhook/deep-search
VITE_N8N_IMAGE_URL=http://localhost:5678/webhook/generate-image
